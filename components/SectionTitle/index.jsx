import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <div>
      <p className="text-purple font-medium font-['Popins'] text-[18px]">
        {title}
      </p>
    </div>
  );
};

export default SectionTitle;
